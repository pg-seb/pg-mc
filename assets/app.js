const toggle=document.querySelector('.mobile-toggle'),nav=document.querySelector('.nav');if(toggle&&nav)toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.classList.toggle('open');toggle.setAttribute('aria-expanded',nav.classList.contains('open'))});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('[data-reveal]').forEach(el=>io.observe(el));
const slides=[...document.querySelectorAll('.home-slider .slide')],dots=[...document.querySelectorAll('.slider-dots button')];let current=0,timer;
function show(n){if(!slides.length)return;slides[current]?.classList.remove('active');dots[current]?.classList.remove('active');current=(n+slides.length)%slides.length;slides[current].classList.add('active');dots[current]?.classList.add('active');clearInterval(timer);timer=setInterval(()=>show(current+1),6500)}
document.querySelector('.slider-arrow.next')?.addEventListener('click',()=>show(current+1));document.querySelector('.slider-arrow.prev')?.addEventListener('click',()=>show(current-1));dots.forEach((d,i)=>d.addEventListener('click',()=>show(i)));if(slides.length)show(0);
